# 2026-09-02

## 提交：客服手机号相关改动（2026-09-02）
- commit `0ad50e3`（6 文件，+57/-3）已推送 origin/main：claim 页/预览工具条/主页页脚/独立站页脚新增手机号 17611535739。
- 顺带确认：Cloudflare Pages 构建输出目录应填 **out**（用户误配成 dist，已纠正）。

## 页脚新增技术支持电话（完成）
- 需求：主页与独立站页脚增加技术支持电话 **17611535739**（微信同号）。
- 修改点：
  - `src/components/Footer.tsx`：品牌区"联系"邮箱下新增"技术支持：17611535739"（tel: 链接）。
  - `src/i18n/site.ts`：`siteI18n.footer` 新增双语文案 `techSupport`（en "Tech support:" / zh "技术支持："）。
  - `src/components/templates/site/CompanySite.tsx`：页脚版权行上方新增"技术支持：17611535739"行（随站点语言中英切换）。
- lint 通过，未 build（纯文本/链接改动）。

## 认领界面新增客服手机号（完成）
- 需求：认领界面新增联系方式，用户不想提交表单可直接电话联系 **17611535739**（微信同号）。
- 修改点：
  - `src/app/(main)/claim/page.tsx`：新增常量 `CONTACT_PHONE`；SectionTitle 下方加浅色提示条"如您不想在线提交认领，可直接电话/微信联系"；表单底部联系方式由纯邮件扩展为"邮件 + 电话/微信"两行（tel: 链接）。
  - `src/app/(preview)/layout.tsx`：预览工具条"预约同款模板建站"后新增"电话 17611535739"链接（桌面端显示）。
- lint 通过，未 build（改动纯文本/链接，无类型影响）。

## B4 深度分析 + D3 优化方案（完成）
- 承接 09-01 跨天会话：1923 家名录池集成（50 家，/registry 页）、T3/T4 16 家核验替换已完成（见 MEMORY.md 进度）。
- **新增 `scripts/analyze-sites.ts`**：批量抓取 companies.ts 全部渠道首页（30 URL），提取技术栈/SEO/转化/语言/服务器头，落盘 `scripts/data/site-analysis.json`（29/30 成功，semisic.cn 为已知 WAF 拦截）。重跑与 eslint/tsc 均通过。
- **新增 `docs/D3-optimization.md`（v0.1）**：B4 四维结论 + 7 项共性差距 + A 模板化重建（P0 朔美/博达/坚博士 + P1 宏艺/雅艺轩/尊屹/中科创源）/ B 渐进优化（12 项清单）/ 决策矩阵 / 优先级分批（P0–P3，不纳入太钢/高科华兴/钢科随集团站）。
- **关键实证结论**：结构化数据 0/30、canonical 9/30、H1 6/30、WhatsApp 2/30（管家营英/天镇通航）、PayPal 0/30、海外社媒 1/30（高科华烨英）、确认英文站仅 3 家（冠力/管家营/高科华烨）；技术栈全部传统定制/纯静态/凡科/疑织梦，无 CMS。
- **4 个疑点站待人工复核**：朔美 smmuye.com（主机默认页"主机开设成功！！！"）、博达 boda-arts/boda-yingxian（双站 923B 同内容跳转页）、坚博士 soliboss.com（917B"首页-企业官网"默认模板页）、高科华烨 gkgd.cn（title 主体"高科华杰"、htmlLang=en 但中文）。
- **companies.ts 已标注 3 个疑点站 note**（朔美/博达/坚博士，仅 note 文本，未动 verifyStatus/url）；头注释补充深度分析说明。
- **文档同步**：site-inventory.md v0.3（结论/短板实证化/待办勾选+疑点复核项）、todo-list.md v0.4（B4 ✅、D3 ✅）、test-plan.md（B4/D3 状态更新）。
- 校验：npm run validate 通过、npx tsc --noEmit 通过、eslint scripts/analyze-sites.ts 通过。**未重新 build**（数据仅 note 文本变化，build 需先重命名 .next/out 防 safe-delete 守卫）。
- 遗留：疑点站人工复核；A1 决策仍是 B1/B2 阻塞点；旧备份目录待清理。

## 企业规模化集成（A1 口径确认 + B1 扩展至 200 家，完成）
- **用户决策**：集成口径 = **4 大类 × 每类 Top50 = 200 家**；名单来源 = 继续公开渠道抓取；待集成体现 = 统计展示 + 待集成池。
- **抓取**：新增 `scripts/fetch-industry.ts` 抓名录集 33 个分类（工厂/专题/开发区/区县），原始 1547 条 → 去重 608 家 → `scripts/data/industry-companies.json`。踩坑：① 区县 URL 必须带"县/市/区"后缀（`/shanxi/county/定襄县`）；② 区县页 views-row 用**双引号** `class="views-row"`（工厂页是单引号）；③ PowerShell 命令行中文/UTF-8 全乱码，node 内联中文脚本不可用，一律写 ts/js 脚本文件执行；④ PowerShell 传参中文编码损坏，脚本过滤用 `--county-only` 布尔参数。
- **数据构建**：`scripts/build-companies-data.ts`（归类规则：区县来源 + 产业关键词，counties 非空时**必须**来自限定区县，杜绝"电子商务"误入光机电）+ `scripts/combine-data.ts`（txt→TS）。
- **companies.ts：40 → 200 家**。新增 `src/data/companies-extra.ts`（160 家：真实归类仅 2 家 = 恒硕法兰/盐湖红浩机电 + 158 家产业名占位如"祁县玻璃·水晶酒具 01"），全部 pending。占位 slug 格式 `{beltSlug}-pNN`，真实归类 `c-belt-N`。companies-extra 不标注类型（避免与 companies.ts 循环 import），template/verifyStatus 用 `as const` 满足 Company 联合类型。
- **industrial-belts.ts**：16 个产业带 companySlugs 按 BELT_TARGETS 目标数（每类 50 分摊）追加（共 160 个 slug）。
- **registry.ts：50 → 270 家**。220 家未归类真实名录入 `src/data/registry-extra.ts`（id 格式 r-1NNN），噪音已过滤（"定位查询附近的公司"等导航文本）。
- **待集成统计**：新增 `src/data/integration.ts`（marketTotal=1963、integratedCount=companies.length=200、pendingCount=1763、registryPoolCount=270）。首页数据看板改为动态"已收录 200 家 / 待集成 1763 家"；/companies 与 /registry 页描述同步更新。
- **验证**：npm run validate 通过（200 企业/16 产业带）、npx tsc --noEmit 通过（src 无错）、**build 成功 426 页**（200 详情 + 200 site + 16 产业带等）、关键文本用 node 脚本抽查 out 产物 OK。
- **坑**：build 会杀掉正在跑的 dev server（.next 被重写）→ 需重启 dev；`Start-Process npm run dev` 后台启动 OK，但注意端口 3000 别重复起两个 dev server（旧进程 14468/42228 仍在 Listen 且状态损坏）。
- **遗留**：158 家占位待官方备案名录批量替换；registry 来源筛选选项 50 个（flex-wrap 可显示）；`out_prev_0902b`/`.next_prev_0902b` 等备份目录待清理（safe-delete 拦截）。
- 文档同步：todo-list.md v0.5（A1 部分完成 🚧、B1 更新、v0.5 更新说明、执行顺序更新）。

## Cloudflare 部署失败定位（Workers/OpenNext 误用，2026-09-02）
- 用户按 Workers 方式部署（`npx wrangler deploy`）→ wrangler 检测 Next.js 自动走 OpenNext 迁移（云端生成 wrangler.jsonc/open-next.config.ts/.dev.vars 并改 package.json/next.config.ts）→ `opennextjs-cloudflare build` 在 Bundling cache assets 步报 ENOENT：`/opt/buildhome/repo/.next/standalone/.next/server/pages-manifest.json`。
- **根因**：本项目 `output:'export'` 纯静态导出，`.next` 无 standalone 也无 pages-manifest.json（App Router 为 app-paths-manifest.json），OpenNext 必然失败。**本项目必须用 Cloudflare Pages（输出目录 out），禁用 Workers/OpenNext**。
- 本地仓库未被污染（无 wrangler.jsonc/open-next.config.ts/.dev.vars，package.json/next.config.ts/.gitignore 原样）。
- 修复方案：Dashboard 删除 jinkuatong Worker 项目 → 新建 **Pages** 项目连 Git（构建命令 `npm run build`、输出目录 `out`、不填部署命令）；CLI 备选 `npx wrangler pages deploy out --project-name jinkuatong`（注意是 `pages deploy` 不是 `deploy`）。

## Cloudflare Pages 首次线上部署成功（2026-09-02）
- 流程：本地构建（先 `Rename-Item .next/.next_prev_dep` + `out/out_prev_dep` 防 safe-delete 守卫）→ `npx wrangler pages project create jinkuatong --production-branch main` → `npx wrangler pages deploy out --project-name jinkuatong --branch main`。
- 结果：上传 891 文件（含 _headers/_redirects），生产 URL **https://jinkuatong.pages.dev/**（本次部署预览 5aff4d36.jinkuatong.pages.dev）；关键页（/ /companies/ /industrial-belts/ /registry/ /about/）全部 HTTP 200。
- 认证：用户 `npx wrangler login` 浏览器 OAuth，账号 `2862430177@qq.com's Account`（id 82c79affa3da6d82fdadc8388acc7c6a）。
- 关键认知：Cloudflare **Pages 与 Workers 是不同命名空间**——之前云端误建的 `jinkuatong` Worker（OpenNext 失败）不影响 Pages 同名项目；`wrangler pages deploy` 要求 Pages 项目先存在，否则报 "Pages project does not exist"（非交互环境部署还需 CLOUDFLARE_API_TOKEN，OAuth 登录后免 token）。
- 待办：后续可给 Pages 项目配置 Git 集成实现 CI 自动部署（F1）；A2 定域后绑定 jinkuatong.com + 构建注入 NEXT_PUBLIC_SITE_URL；E4 Lighthouse 线上验证。

## 备份目录清理 + 代码提交（2026-09-02）
- 清理 8 个备份目录：out_old / out_prev / out_prev_0901 / out_prev_0902 / out_prev_0902b / out_prev_dep / .next_prev_dep / .next_prev_0901，全部删除成功（Remove-Item -Recurse -Force 未被守卫拦截，用户在线确认）。遗留备份目录清零。
- .gitignore 补充 `/out_prev_*/` 通配（与 `/.next_prev_*/` 对齐），防今后 out_prev_xxx 被 git add 误提交。
- commit `2c67370` 已推送到 GitHub（main），工作区 clean。
- 踩坑复现：PowerShell 命令行 commit message 传中文 100% 乱码（解析报错），commit message 须用英文。

## Git 初始化并推送 GitHub（完成）
- 项目首次接入 git：`git init -b main`，初始提交 `9f89977`（93 文件，28170 行），远程 **https://github.com/2862430177/jinkuatong.git**，main 已推送、本地与 origin/main 同步。
- `.gitignore` 补充忽略：全部构建备份目录（`/out_old/`、`/out_prev/`、`/out_prev_0901/`、`/out_prev_0902/`、`/out_prev_0902b/`、`/.next_prev_*/`）、`dev.err.log`、临时文件 `x[1])`（gitignore 需转义为 `x\[1\]\)`）。
- 坑：`git add .` 会把点开头的 `.next_prev_*` 备份目录一并暂存（list_dir 不显示点目录，`git status` 才能发现），需 `git rm --cached -r` 撤出后再 add。

## 询盘方案B（已核验邮箱路由 + 兜底 gmail）+ 平台邮箱统一（完成，未提交）
- **决策/需求**：参考元宝方案B —— 企业独立站有已核验邮箱 → 询盘直达企业邮箱；无核验邮箱 → 默认兜底收件人 **hewei.419763673@gmail.com**；询盘附带来源 **https://jinkuatong.pages.dev/**；平台联系邮箱统一为 gmail。
- **改动**：
  - 新增 `src/data/verified-emails.ts`：`VERIFIED_COMPANY_EMAILS`（已核验名单，当前为空，勿录"猜测的 sales@域名"）+ `FALLBACK_INQUIRY_EMAIL`=gmail + `INQUIRY_SOURCE_URL`=pages.dev。
  - `src/components/templates/site/InquiryForm.tsx`：`getInquiryEmail` 改为名单路由（`VERIFIED_COMPANY_EMAILS[slug] ?? FALLBACK`，弃用 sales@猜测）；mailto 正文附加 Inquiry source 与 Company profile（兜底收件时能识别归属企业）。
  - 平台对外邮箱 hello@jinkuatong.com → hewei.419763673@gmail.com：Footer/claim CONTACT_EMAIL/terms/privacy/layout JSON-LD email。
- **验证**：typecheck ✅、lint 0 错误、validate ✅、**build 成功 430 页**；out 产物抽查：gmail 428 处、Inquiry source 1 处（client chunk）、hello@/猜测 jinkuatong.site 邮箱 0 残留。
- **坑**：本次首次 build 因 dev 进程占用导致 `.next` 预重命名失败、next build 覆写触发 safe-delete 守卫（turn 500 删除达阈值）中断；杀掉残留 node + 重命名 `.next` 后全新 build 通过。**build 前务必确认无 node 占用 `.next`，并先 Rename-Item .next/.out**。
- 遗留：`.next_prev_0902c`/`.next_prev_0902c_broken`/`out_prev_0902c` 备份待清理；改动未提交 git；方案B 完整版（Cloudflare Pages Function + Resend 自动发信）需 A4 决策 + RESEND_API_KEY 后再实施。
